import { testCircle } from "../../src/constants/constants";

// export const testStringReverse = (value, color) => {
//   cy.get(testCircle).each(($el, index) => {
//     cy.get($el)
//       .should("have.css", "border-color", value[index])
//       .should("contain", color[index]);
//   });
// };